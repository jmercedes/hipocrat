<%= render 'refinery/admin/search_header', :url => refinery.job_posts_admin_job_posts_path %>
<div class='pagination_container'>
  <% if @job_posts.any? %>
    <%= render 'job_posts' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
